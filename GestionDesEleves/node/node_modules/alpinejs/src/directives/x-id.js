import { directive } from "alpinejs/src/directives"
import { setIdRoot } from 'alpinejs/src/ids'

directive('id', (el, { expression }, { evaluate }) => {
    let names = evaluate(expression)
    
    names.forEach(name => setIdRoot(el, name))
})
